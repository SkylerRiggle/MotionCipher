{
  "scripts": {
    "build": "npx tsc",
    "start": "npm run build && node ./build/index.js",
    "deploy": "npm run build && gh-pages -d ./build",
    "dev": "nodemon ./index.ts"
  },
  "devDependencies": {
    "gh-pages": "^6.1.1",
    "nodemon": "^3.1.4",
    "ts-node": "^10.9.2",
    "typescript": "^5.5.3"
  }
}
